import type { ColumnMapping } from './types.js'

/**
 * Tier 3: AI-based column mapping (stub).
 * Returns null for all columns — to be implemented in a future version.
 */
export function aiMapColumn(_header: string): ColumnMapping | null {
  return null
}
